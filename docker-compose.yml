services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: "file:/data/app.db"
      AUTH_URL: "${AUTH_URL:-http://localhost:3000}"
    volumes:
      - habit_sqlite:/data

volumes:
  habit_sqlite:
